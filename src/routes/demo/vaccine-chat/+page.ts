import type { PageLoad } from "./$types";

// Standalone funder demo of the combined flu + COVID chatbot. Fixed condition,
// fixed theme; no query parameters are read. Runs top-level (no survey parent).
export const load: PageLoad = () => ({});
